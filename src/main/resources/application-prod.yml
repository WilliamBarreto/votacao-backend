spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysqlsrv:3306/votacao_db?currentSchema=votacao_db
    username: root
    password: Pwd@2021
kafka.server: kafka:9092
